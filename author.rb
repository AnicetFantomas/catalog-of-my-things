class Author
    def initialize(first_name, last_name, items)
        @id = Random.rand(1..1000)
        @first_name = first_name
        @last_name = last_name
        items = []
    end

    def add_item
    end
end